#include<iostream>

using Int = long long int;

void kombinacje(Int a, Int b, std::ostream& out) {
  for(;;)
}

int main() {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(0);

  Int n;
  std::cin >> n;
  kombinacje(1, n, std::cout);

  return 0;
}

