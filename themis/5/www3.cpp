#include<iostream>
#include<vector>

using Int = long long int;

int main() {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(0);

  std::vector<Int> g[100000];

  Int n;
  std::cin >> n;
  for(Int i=1; i<n; ++i) {
  }

  Int q;
  std::cin >> q;
  for(Int i=0; i<q; ++i) {
  }

  return 0;
}

